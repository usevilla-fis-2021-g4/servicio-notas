apiVersion: v1
kind: Secret
metadata:
  name: mongodb
  labels:
    app.kubernetes.io/component: mongodb
    {{- include "backendnotas.labels" . | nindent 4 }}
type: Opaque
data:
  notas_mongo_url: {{ .Values.notasMongoUrl | b64enc }}